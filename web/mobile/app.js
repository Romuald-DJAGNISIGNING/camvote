const copy = {
  en: {
    brandTag: 'Official civic platform',
    pill: 'Mobile-first access',
    heroTitle: 'Your vote, in your pocket.',
    heroSubtitle: 'The CamVote mobile app lets citizens register, verify, vote, and receive official election updates anywhere.',
    ctaPlay: 'Get it on Google Play',
    ctaApp: 'Download on the App Store',
    badgeSecurity: 'Biometric security',
    badgeRealtime: 'Realtime alerts',
    badgeLowBandwidth: 'Low-bandwidth ready',
    platformsTitle: 'Built for every device',
    platformsSubtitle: 'Native Android and iOS experiences designed for secure, low-bandwidth civic access.',
    platformAndroid: 'Android optimized',
    platformAndroidDesc: 'Fast onboarding, low-bandwidth mode, and device security built-in.',
    platformIos: 'iOS refined',
    platformIosDesc: 'Smooth verification, secure biometrics, and elegant civic workflows.',
    trustBadge1: 'Device-bound signatures',
    trustBadge2: 'Encrypted receipts',
    trustBadge3: 'Biometric verification',
    trustBadge4: 'Official civic data',
    trustBadge5: 'Fraud monitoring',
    screenTitle: 'Election Day',
    screenSub: 'Countdowns, updates, and your card.',
    metric1Label: 'Next deadline',
    metric1Value: 'Soon',
    metric2Label: 'Coverage',
    metric2Value: 'Nationwide',
    floatTitle1: 'Official sources',
    floatBody1: 'Verified civic information',
    floatTitle2: 'Live updates',
    floatBody2: 'Results & alerts',
    featuresTitle: 'Everything you need to participate',
    iconGridTitle: 'Feature highlights',
    iconGridSubtitle: 'Visual cues for the most important CamVote experiences.',
    iconItem1: 'Secure identity',
    iconItem2: 'OCR scan',
    iconItem3: 'Live countdowns',
    iconItem4: 'Global map',
    iconItem5: 'Civic education',
    iconItem6: 'Official alerts',
    iconItem7: 'Eligibility tracking',
    iconItem8: 'Secure receipts',
    showcaseTitle: 'A mobile experience you can feel',
    showcaseSubtitle: 'Every flow is designed to be calm, guided, and secure.',
    showcaseCard1Chip: 'Registration',
    showcaseCard1Title: 'Registration, simplified',
    showcaseCard1Body: 'Smart forms and OCR guide citizens through every step.',
    showcaseCard2Chip: 'Verification',
    showcaseCard2Title: 'Verification you can trust',
    showcaseCard2Body: 'Biometrics and device keys lock each identity to the device.',
    showcaseCard3Chip: 'Results',
    showcaseCard3Title: 'Results and transparency',
    showcaseCard3Body: 'Live updates, regional maps, and official summaries.',
    tickerTitle: 'Live election ticker',
    tickerSubtitle: 'Updates, alerts, and milestones roll in real time.',
    tickerItem1: 'Registration deadline approaching',
    tickerItem2: 'New polling centers added',
    tickerItem3: 'Eligibility unlocked for new voters',
    tickerItem4: 'Results preview available',
    countdownTitle: 'Countdown intelligence',
    countdownSubtitle: 'Personalized timers keep every citizen ready.',
    countdownItem1Title: 'Election opens',
    countdownItem1Body: 'Live countdown with opening alerts.',
    countdownItem2Title: 'Election closes',
    countdownItem2Body: 'Closing reminders and confirmation.',
    countdownItem3Title: 'Card renewal',
    countdownItem3Body: 'e-Card expiry warnings and renewal action.',
    countdownItem4Title: 'Eligibility unlock',
    countdownItem4Body: 'Celebrate the moment you become eligible.',
    storyTitle: 'The full citizen journey',
    storySubtitle: 'From onboarding to results, everything stays in one calm flow.',
    story1Chip: 'Onboarding',
    story1Title: 'Warm onboarding',
    story1Body: 'Guided steps help every voter feel supported.',
    story2Chip: 'Identity',
    story2Title: 'Identity locked',
    story2Body: 'Biometrics ensure each voter stays unique.',
    story3Chip: 'Vote',
    story3Title: 'Ballot confidence',
    story3Body: 'Clear candidates, secure cast, instant receipt.',
    story4Chip: 'Receipt',
    story4Title: 'Private receipt',
    story4Body: 'Encrypted proofs for your records.',
    story5Chip: 'Results',
    story5Title: 'Live results',
    story5Body: 'Transparent results and regional maps.',
    galleryTitle: 'Screen gallery',
    gallerySubtitle: 'A quick tour of the core CamVote flows.',
    galleryLabel1: 'Home dashboard',
    galleryLabel2: 'Verification timeline',
    galleryLabel3: 'Countdowns',
    galleryLabel4: 'Centers map',
    galleryLabel5: 'Results',
    screenwallTitle: 'Interactive screen previews',
    screenwallSubtitle: 'Slide through real CamVote flows on Samsung Galaxy S24 Ultra and iPhone 17 Pro.',
    deviceLabelAndroid: 'Samsung Galaxy S24 Ultra',
    deviceLabelIos: 'iPhone 17 Pro',
    screen1Title: 'Citizen dashboard',
    screen1Sub: 'Verified profile',
    screen1Chip: 'Verified',
    screen1CardTitle: 'Election countdown',
    screen1Open: 'Opens in',
    screen1Close: 'Closes in',
    screen1Action1: 'Register',
    screen1Action2: 'Vote',
    screen1Action3: 'Map',
    screen1Action4: 'Results',
    screen2Title: 'Registration hub',
    screen2Sub: 'Step-by-step guidance',
    screen2Chip: 'Onboarding',
    screen2Step1: 'Document scan',
    screen2Step2: 'Biometrics',
    screen2Step3: 'Review & submit',
    screen2Status: 'In progress',
    screen3Title: 'Document OCR',
    screen3Sub: 'Auto capture and extraction',
    screen3Chip: 'OCR',
    screen3ScanTitle: 'ID scan',
    screen3ScanHint: 'Hold steady for auto-capture',
    screen3Field1: 'Full name',
    screen3Field2: 'Document ID',
    screen3Field3: 'Date of birth',
    screen4Title: 'Biometric check',
    screen4Sub: 'Face + liveness verification',
    screen4Chip: 'Secure',
    screen4FaceTitle: 'Align your face',
    screen4FaceHint: 'Follow the on-screen guide',
    screen4Step1: 'Device bound keys',
    screen4Step2: 'Liveness scan',
    screen4Step3: 'Identity locked',
    screen5Title: 'Verification status',
    screen5Sub: 'We are reviewing your profile',
    screen5Chip: 'Review',
    screen5Stage1: 'Submitted',
    screen5Stage2: 'Verification',
    screen5Stage3: 'Approved',
    screen5Status: 'Next update in 24h',
    screen6Title: 'Election calendar',
    screen6Sub: 'Upcoming civic events',
    screen6Chip: 'Upcoming',
    screen6Event1: 'Municipal elections',
    screen6Event2: 'Registration closes',
    screen6Event3: 'Card renewal',
    screen6CountdownLabel: 'Personal countdowns active',
    screen7Title: 'Cast your vote',
    screen7Sub: 'Secure ballot selection',
    screen7Chip: 'Secure',
    screen7Candidate1: 'Candidate A',
    screen7Candidate2: 'Candidate B',
    screen7Candidate3: 'Candidate C',
    screen7Action: 'Cast vote',
    screen8Title: 'Vote receipt',
    screen8Sub: 'Encrypted proof stored locally',
    screen8Chip: 'Receipt',
    screen8Line1: 'Receipt ID',
    screen8Line2: 'Status',
    screen8Line3: 'Hash',
    screen8Status: 'Saved to device',
    screen9Title: 'Live results',
    screen9Sub: 'Transparent regional updates',
    screen9Chip: 'Live',
    screen9CardTitle: 'Regional results',
    screen9Stat1: 'Turnout',
    screen9Stat2: 'Incidents',
    screen9Stat3: 'Map view',
    screen10Title: 'Polling centers',
    screen10Sub: 'Cameroon + diaspora map',
    screen10Chip: 'Map',
    screen10Point1: 'Yaoundé center',
    screen10Point2: 'Embassy access',
    screen11Title: 'Notifications',
    screen11Sub: 'Official alerts and updates',
    screen11Chip: 'Live',
    screen11Notif1: 'Polling center updated',
    screen11Notif2: 'Eligibility unlocked',
    screen11Notif3: 'Results published',
    screen12Title: 'Profile & settings',
    screen12Sub: 'Manage your account',
    screen12Chip: 'Settings',
    screen12Setting1: 'Biometric lock',
    screen12Setting2: 'Notifications',
    screen12Setting3: 'Language',
    screen12Action: 'Get support',
    pillar1Title: 'Smart registration',
    pillar1Body: 'Guided steps with OCR and verification to avoid errors.',
    pillar2Title: 'Trusted voting',
    pillar2Body: 'Device-bound signatures, biometrics, and audit trails.',
    pillar3Title: 'Civic clarity',
    pillar3Body: 'Legal texts, civic education, and transparency tools.',
    metricsTitle: 'Built for scale',
    metricsSubtitle: 'Designed to support nationwide participation and diaspora voters.',
    metricCard1: 'Regions covered',
    metricCard2: 'Countries reachable',
    metricCard3: '/7 official updates',
    metricCard4: '% audit visibility',
    integrityTitle: 'Security you can see',
    integrityBody: 'Every action is signed, verified, and audited with device-bound security.',
    integrityItem1: 'Device-bound cryptography',
    integrityItem2: 'Biometric check-in',
    integrityItem3: 'Fraud monitoring',
    integrityItem4: 'Audit-ready history',
    integrityBadge: 'Protected',
    journeyTitle: 'A guided journey',
    journeyBody: 'From onboarding to voting day, the app keeps citizens aligned and confident.',
    journey1: 'Scan documents, verify identity, and secure your device.',
    journey2: 'Find polling centers in Cameroon or abroad.',
    journey3: 'Track eligibility, card renewal, and election countdowns.',
    step1Title: 'Download',
    step1Body: 'Install the official CamVote app from the store.',
    step2Title: 'Verify',
    step2Body: 'Create your secure device profile with biometrics.',
    step3Title: 'Participate',
    step3Body: 'Register, vote, and track results on the go.',
    coverageTitle: 'Nationwide + diaspora coverage',
    coverageBody: 'Polling centers, embassies, and diaspora hubs stay visible for every citizen.',
    coveragePoint1: 'Add or update polling centers fast.',
    coveragePoint2: 'Support for voters abroad.',
    coveragePoint3: 'Map-first discovery and guidance.',
    coverageTag1: 'Admin-managed',
    coverageTag2: 'Always current',
    coverageTag3: 'Global access',
    receiptTitle: 'Your secure receipt',
    receiptBody: 'Every vote generates a private, verifiable receipt stored securely on your device.',
    receiptTag1: 'Encrypted',
    receiptTag2: 'Private',
    receiptTag3: 'Verifiable',
    receiptHeader: 'CamVote Receipt',
    receiptLine1: 'Election',
    receiptLine1Value: 'General',
    receiptLine2: 'Status',
    receiptLine2Value: 'Verified',
    receiptLine3: 'Timestamp',
    receiptLine3Value: 'Secure',
    supportTitle: 'Always supported',
    supportBody: 'Help and guidance are built into the app for every voter.',
    supportItem1: 'Help center & FAQs',
    supportItem2: 'Report incidents fast',
    supportItem3: 'Accessibility-first flows',
    supportItem4: 'Localized guidance',
    trustTitle: 'Built for trust',
    trustBody: 'CamVote uses device-bound security, transparent results, and official civic sources so citizens can act with confidence.',
    faqTitle: 'Frequently asked',
    faqSubtitle: 'Quick answers for citizens and first-time voters.',
    faqQ1: 'Is the app official?',
    faqA1: 'Yes. CamVote consolidates official civic sources and verified election updates.',
    faqQ2: 'Is my vote private?',
    faqA2: 'Your vote is protected with device-bound cryptography and never shared publicly.',
    faqQ3: 'Can I vote from abroad?',
    faqA3: 'Yes. The app includes diaspora polling centers and embassy locations.',
    faqQ4: 'What if my card expires?',
    faqA4: 'You will receive renewal warnings and guided steps to update your card.',
    ctaTitle: 'Ready to install?',
    ctaBody: 'Choose your store and get started in minutes.',
    qrTitle: 'Scan to install',
    qrHint: 'Opens the right store for your device',
    scanTitle: 'Opening the right store',
    scanSubtitle: 'Just a second — almost there.',
    footer: 'Official civic participation platform.',
    footerAttributions: 'Attributions'
  },
  fr: {
    brandTag: 'Plateforme civique officielle',
    pill: 'Accès mobile d’abord',
    heroTitle: 'Votre vote, dans votre poche.',
    heroSubtitle: 'L’application mobile CamVote permet de s’inscrire, se vérifier, voter et recevoir des mises à jour officielles partout.',
    ctaPlay: 'Disponible sur Google Play',
    ctaApp: 'Télécharger sur l’App Store',
    badgeSecurity: 'Sécurité biométrique',
    badgeRealtime: 'Alertes en temps réel',
    badgeLowBandwidth: 'Optimisé faible débit',
    platformsTitle: 'Conçue pour chaque appareil',
    platformsSubtitle: 'Des expériences Android et iOS pensées pour un accès civique sécurisé et fluide.',
    platformAndroid: 'Android optimisé',
    platformAndroidDesc: 'Onboarding rapide, mode faible débit, sécurité intégrée.',
    platformIos: 'iOS raffiné',
    platformIosDesc: 'Vérification fluide, biométrie sécurisée, parcours élégants.',
    trustBadge1: 'Signatures liées à l’appareil',
    trustBadge2: 'Reçus chiffrés',
    trustBadge3: 'Vérification biométrique',
    trustBadge4: 'Données civiques officielles',
    trustBadge5: 'Surveillance anti-fraude',
    screenTitle: 'Jour d’élection',
    screenSub: 'Comptes à rebours, alertes et carte.',
    metric1Label: 'Prochaine échéance',
    metric1Value: 'Bientôt',
    metric2Label: 'Couverture',
    metric2Value: 'Nationale',
    floatTitle1: 'Sources officielles',
    floatBody1: 'Information civique vérifiée',
    floatTitle2: 'Mises à jour',
    floatBody2: 'Résultats & alertes',
    featuresTitle: 'Tout pour participer',
    iconGridTitle: 'Points forts',
    iconGridSubtitle: 'Repères visuels pour les expériences clés de CamVote.',
    iconItem1: 'Identité sécurisée',
    iconItem2: 'Scan OCR',
    iconItem3: 'Comptes à rebours',
    iconItem4: 'Carte mondiale',
    iconItem5: 'Éducation civique',
    iconItem6: 'Alertes officielles',
    iconItem7: 'Suivi d’éligibilité',
    iconItem8: 'Reçus sécurisés',
    showcaseTitle: 'Une expérience mobile qui se ressent',
    showcaseSubtitle: 'Chaque parcours est calme, guidé et sécurisé.',
    showcaseCard1Chip: 'Inscription',
    showcaseCard1Title: 'Inscription simplifiée',
    showcaseCard1Body: 'Formulaires intelligents et OCR pour guider chaque étape.',
    showcaseCard2Chip: 'Vérification',
    showcaseCard2Title: 'Vérification de confiance',
    showcaseCard2Body: 'Biométrie et clés d’appareil pour verrouiller l’identité.',
    showcaseCard3Chip: 'Résultats',
    showcaseCard3Title: 'Résultats et transparence',
    showcaseCard3Body: 'Mises à jour en direct, cartes régionales et synthèses.',
    tickerTitle: 'Fil d’actualité électorale',
    tickerSubtitle: 'Mises à jour, alertes et jalons en temps réel.',
    tickerItem1: 'Date limite d’inscription approche',
    tickerItem2: 'Nouveaux centres de vote ajoutés',
    tickerItem3: 'Éligibilité activée pour les nouveaux électeurs',
    tickerItem4: 'Aperçu des résultats disponible',
    countdownTitle: 'Intelligence des comptes à rebours',
    countdownSubtitle: 'Des minuteurs personnalisés pour rester prêt.',
    countdownItem1Title: 'Ouverture du vote',
    countdownItem1Body: 'Compte à rebours et alertes d’ouverture.',
    countdownItem2Title: 'Clôture du vote',
    countdownItem2Body: 'Rappels de clôture et confirmation.',
    countdownItem3Title: 'Renouvellement carte',
    countdownItem3Body: 'Alertes d’expiration et action rapide.',
    countdownItem4Title: 'Déblocage éligibilité',
    countdownItem4Body: 'Célébrez le passage à l’éligibilité.',
    storyTitle: 'Le parcours citoyen complet',
    storySubtitle: 'De l’onboarding aux résultats, tout reste fluide.',
    story1Chip: 'Onboarding',
    story1Title: 'Onboarding fluide',
    story1Body: 'Étapes guidées pour chaque électeur.',
    story2Chip: 'Identité',
    story2Title: 'Identité verrouillée',
    story2Body: 'La biométrie garantit l’unicité.',
    story3Chip: 'Vote',
    story3Title: 'Vote en confiance',
    story3Body: 'Candidats clairs, vote sécurisé, reçu immédiat.',
    story4Chip: 'Reçu',
    story4Title: 'Reçu privé',
    story4Body: 'Preuves chiffrées pour vos archives.',
    story5Chip: 'Résultats',
    story5Title: 'Résultats en direct',
    story5Body: 'Résultats transparents et cartes régionales.',
    galleryTitle: 'Galerie des écrans',
    gallerySubtitle: 'Un aperçu rapide des parcours clés CamVote.',
    galleryLabel1: 'Tableau de bord',
    galleryLabel2: 'Chronologie de vérification',
    galleryLabel3: 'Comptes à rebours',
    galleryLabel4: 'Carte des centres',
    galleryLabel5: 'Résultats',
    screenwallTitle: 'Aperçus interactifs',
    screenwallSubtitle: 'Faites défiler les parcours CamVote sur Samsung Galaxy S24 Ultra et iPhone 17 Pro.',
    deviceLabelAndroid: 'Samsung Galaxy S24 Ultra',
    deviceLabelIos: 'iPhone 17 Pro',
    screen1Title: 'Tableau citoyen',
    screen1Sub: 'Profil vérifié',
    screen1Chip: 'Vérifié',
    screen1CardTitle: 'Compte à rebours',
    screen1Open: 'Ouverture dans',
    screen1Close: 'Clôture dans',
    screen1Action1: 'Inscription',
    screen1Action2: 'Vote',
    screen1Action3: 'Carte',
    screen1Action4: 'Résultats',
    screen2Title: 'Hub d’inscription',
    screen2Sub: 'Parcours étape par étape',
    screen2Chip: 'Onboarding',
    screen2Step1: 'Scan des documents',
    screen2Step2: 'Biométrie',
    screen2Step3: 'Revue & envoi',
    screen2Status: 'En cours',
    screen3Title: 'OCR documents',
    screen3Sub: 'Capture et extraction auto',
    screen3Chip: 'OCR',
    screen3ScanTitle: 'Scan pièce',
    screen3ScanHint: 'Restez stable pour l’auto-capture',
    screen3Field1: 'Nom complet',
    screen3Field2: 'ID document',
    screen3Field3: 'Date de naissance',
    screen4Title: 'Contrôle biométrique',
    screen4Sub: 'Visage + preuve de vie',
    screen4Chip: 'Sécurisé',
    screen4FaceTitle: 'Alignez votre visage',
    screen4FaceHint: 'Suivez le guide à l’écran',
    screen4Step1: 'Clés liées à l’appareil',
    screen4Step2: 'Scan de vivacité',
    screen4Step3: 'Identité verrouillée',
    screen5Title: 'Statut de vérification',
    screen5Sub: 'Votre profil est en cours de revue',
    screen5Chip: 'Revue',
    screen5Stage1: 'Soumis',
    screen5Stage2: 'Vérification',
    screen5Stage3: 'Approuvé',
    screen5Status: 'Prochaine mise à jour dans 24h',
    screen6Title: 'Calendrier électoral',
    screen6Sub: 'Événements à venir',
    screen6Chip: 'À venir',
    screen6Event1: 'Élections municipales',
    screen6Event2: 'Clôture des inscriptions',
    screen6Event3: 'Renouvellement carte',
    screen6CountdownLabel: 'Comptes à rebours actifs',
    screen7Title: 'Votez',
    screen7Sub: 'Sélection du bulletin sécurisée',
    screen7Chip: 'Sécurisé',
    screen7Candidate1: 'Candidat A',
    screen7Candidate2: 'Candidat B',
    screen7Candidate3: 'Candidat C',
    screen7Action: 'Voter',
    screen8Title: 'Reçu de vote',
    screen8Sub: 'Preuve chiffrée stockée',
    screen8Chip: 'Reçu',
    screen8Line1: 'ID reçu',
    screen8Line2: 'Statut',
    screen8Line3: 'Hash',
    screen8Status: 'Enregistré sur l’appareil',
    screen9Title: 'Résultats en direct',
    screen9Sub: 'Mises à jour régionales',
    screen9Chip: 'Direct',
    screen9CardTitle: 'Résultats régionaux',
    screen9Stat1: 'Participation',
    screen9Stat2: 'Incidents',
    screen9Stat3: 'Vue carte',
    screen10Title: 'Centres de vote',
    screen10Sub: 'Carte Cameroun + diaspora',
    screen10Chip: 'Carte',
    screen10Point1: 'Centre de Yaoundé',
    screen10Point2: 'Accès ambassades',
    screen11Title: 'Notifications',
    screen11Sub: 'Alertes et mises à jour officielles',
    screen11Chip: 'Direct',
    screen11Notif1: 'Centre de vote mis à jour',
    screen11Notif2: 'Éligibilité débloquée',
    screen11Notif3: 'Résultats publiés',
    screen12Title: 'Profil & réglages',
    screen12Sub: 'Gérez votre compte',
    screen12Chip: 'Réglages',
    screen12Setting1: 'Verrou biométrique',
    screen12Setting2: 'Notifications',
    screen12Setting3: 'Langue',
    screen12Action: 'Obtenir de l’aide',
    pillar1Title: 'Inscription intelligente',
    pillar1Body: 'Étapes guidées avec OCR et vérification pour éviter les erreurs.',
    pillar2Title: 'Vote fiable',
    pillar2Body: 'Signatures liées à l’appareil, biométrie et audits.',
    pillar3Title: 'Clarté civique',
    pillar3Body: 'Textes juridiques, éducation civique et transparence.',
    metricsTitle: 'Conçue pour l’échelle',
    metricsSubtitle: 'Pensée pour la participation nationale et la diaspora.',
    metricCard1: 'Régions couvertes',
    metricCard2: 'Pays accessibles',
    metricCard3: 'Mises à jour officielles /7',
    metricCard4: 'Visibilité d’audit (%)',
    integrityTitle: 'Sécurité visible',
    integrityBody: 'Chaque action est signée, vérifiée et auditée par l’appareil.',
    integrityItem1: 'Cryptographie liée à l’appareil',
    integrityItem2: 'Validation biométrique',
    integrityItem3: 'Détection de fraude',
    integrityItem4: 'Historique auditable',
    integrityBadge: 'Protégé',
    journeyTitle: 'Un parcours guidé',
    journeyBody: 'De l’onboarding au jour du vote, l’app vous accompagne.',
    journey1: 'Scannez les documents, vérifiez l’identité, sécurisez l’appareil.',
    journey2: 'Trouvez les bureaux au Cameroun ou à l’étranger.',
    journey3: 'Suivez éligibilité, renouvellement et comptes à rebours.',
    step1Title: 'Télécharger',
    step1Body: 'Installez l’application officielle CamVote depuis le store.',
    step2Title: 'Vérifier',
    step2Body: 'Créez votre profil sécurisé avec biométrie.',
    step3Title: 'Participer',
    step3Body: 'Inscrivez-vous, votez et suivez les résultats.',
    coverageTitle: 'Couverture nationale + diaspora',
    coverageBody: 'Centres de vote, ambassades et hubs diaspora visibles pour tous.',
    coveragePoint1: 'Ajouter ou mettre à jour les centres rapidement.',
    coveragePoint2: 'Support pour les électeurs à l’étranger.',
    coveragePoint3: 'Découverte et guidage par carte.',
    coverageTag1: 'Géré par l’admin',
    coverageTag2: 'Toujours à jour',
    coverageTag3: 'Accès global',
    receiptTitle: 'Votre reçu sécurisé',
    receiptBody: 'Chaque vote génère un reçu privé et vérifiable sur votre appareil.',
    receiptTag1: 'Chiffré',
    receiptTag2: 'Privé',
    receiptTag3: 'Vérifiable',
    receiptHeader: 'Reçu CamVote',
    receiptLine1: 'Élection',
    receiptLine1Value: 'Générale',
    receiptLine2: 'Statut',
    receiptLine2Value: 'Vérifié',
    receiptLine3: 'Horodatage',
    receiptLine3Value: 'Sécurisé',
    supportTitle: 'Toujours accompagné',
    supportBody: 'Aide et guidance intégrées pour chaque électeur.',
    supportItem1: 'Centre d’aide & FAQ',
    supportItem2: 'Signaler des incidents rapidement',
    supportItem3: 'Parcours accessibles',
    supportItem4: 'Guides localisés',
    trustTitle: 'Conçue pour la confiance',
    trustBody: 'CamVote combine sécurité liée à l’appareil, résultats transparents et sources officielles.',
    faqTitle: 'Questions fréquentes',
    faqSubtitle: 'Réponses rapides pour citoyens et nouveaux électeurs.',
    faqQ1: 'L’application est-elle officielle ?',
    faqA1: 'Oui. CamVote rassemble des sources civiques officielles et des mises à jour vérifiées.',
    faqQ2: 'Mon vote est-il privé ?',
    faqA2: 'Votre vote est protégé par cryptographie liée à l’appareil.',
    faqQ3: 'Puis-je voter depuis l’étranger ?',
    faqA3: 'Oui. L’app inclut les centres diaspora et les ambassades.',
    faqQ4: 'Et si ma carte expire ?',
    faqA4: 'Vous recevez des alertes de renouvellement et des étapes guidées.',
    ctaTitle: 'Prêt à installer ?',
    ctaBody: 'Choisissez votre store et démarrez en quelques minutes.',
    qrTitle: 'Scannez pour installer',
    qrHint: 'Ouvre la boutique adaptee a votre appareil',
    scanTitle: 'Ouverture de la boutique',
    scanSubtitle: 'Un instant, presque pret.',
    footer: 'Plateforme officielle de participation civique.',
    footerAttributions: 'Attributions'
  }
};

const featureData = {
  en: [
    { title: 'Secure device identity', body: 'Each device generates a protected key pair tied to your profile.' },
    { title: 'Smart OCR onboarding', body: 'Scan IDs and auto-fill registration details safely.' },
    { title: 'Election countdowns', body: 'See openings, closings, and deadlines in one glance.' },
    { title: 'Voting centers map', body: 'Locate polling stations in Cameroon and abroad.' },
    { title: 'Civic education hub', body: 'Learn your rights, processes, and legal texts.' },
    { title: 'Trusted notifications', body: 'Get official alerts, results, and incident updates.' },
    { title: 'Eligibility tracking', body: 'Stay informed about status changes and renewals.' },
    { title: 'Secure receipts', body: 'Keep a private, verifiable record of your participation.' },
    { title: 'Multilingual access', body: 'French + English support for every voter.' },
    { title: 'Offline resilience', body: 'Low-bandwidth ready for constrained areas.' }
  ],
  fr: [
    { title: 'Identité d’appareil sécurisée', body: 'Chaque appareil crée une paire de clés protégée.' },
    { title: 'Onboarding OCR intelligent', body: 'Scannez les pièces et remplissez l’inscription.' },
    { title: 'Comptes à rebours', body: 'Ouvertures, clôtures et échéances en un coup d’œil.' },
    { title: 'Carte des centres', body: 'Localisez les bureaux au Cameroun et à l’étranger.' },
    { title: 'Hub d’éducation civique', body: 'Apprenez vos droits, procédures et lois.' },
    { title: 'Notifications fiables', body: 'Recevez alertes officielles, résultats et incidents.' },
    { title: 'Suivi d’éligibilité', body: 'Restez informé des changements et renouvellements.' },
    { title: 'Reçus sécurisés', body: 'Gardez une preuve privée et vérifiable de participation.' },
    { title: 'Accès multilingue', body: 'Français + anglais pour chaque citoyen.' },
    { title: 'Résilience hors-ligne', body: 'Optimisé pour les zones à faible débit.' }
  ]
};

const DEFAULT_PLAY = 'https://play.google.com/store/apps/details?id=com.camvote.app';
const DEFAULT_APP = '/mobile/app-store/';
const DEFAULT_PUBLIC = '/#/public';
const AUTO_REDIRECT_DELAY_MS = 1450;
const BURST_PARTICLE_COUNT = 12;

let currentLang = 'en';
let revealObserver;
let autoRedirectStarted = false;

function applyLanguage(lang) {
  const dict = copy[lang] || copy.en;
  currentLang = lang;
  document.documentElement.lang = lang;
  document.querySelectorAll('[data-i18n]').forEach((el) => {
    const key = el.getAttribute('data-i18n');
    if (dict[key]) {
      el.textContent = dict[key];
    }
  });

  const grid = document.getElementById('featureGrid');
  if (grid) {
    grid.innerHTML = '';
    (featureData[lang] || featureData.en).forEach((feature) => {
      const card = document.createElement('div');
      card.className = 'feature reveal';
      card.innerHTML = `<h3>${feature.title}</h3><p>${feature.body}</p>`;
      grid.appendChild(card);
    });
  }

  document.querySelectorAll('.lang-btn').forEach((btn) => {
    btn.classList.toggle('active', btn.dataset.lang === lang);
  });

  observeReveals();
}

function setStoreLinks() {
  const params = new URLSearchParams(window.location.search);
  const play = params.get('play') || DEFAULT_PLAY;
  const iosLive = params.get('ios_live') === '1';
  const requestedApp = params.get('app') || DEFAULT_APP;
  const app = iosLive ? requestedApp : DEFAULT_APP;
  const publicUrl = params.get('public') || DEFAULT_PUBLIC;
  const auto = params.get('auto');
  const deeplinkAndroid = params.get('deeplink_android') || params.get('deeplink');
  const deeplinkIos = params.get('deeplink_ios') || params.get('deeplink');
  const playCta = document.getElementById('playCta');
  const appCta = document.getElementById('appCta');
  const playCtaBottom = document.getElementById('playCtaBottom');
  const appCtaBottom = document.getElementById('appCtaBottom');
  const smartQr = document.getElementById('smartQr');
  const qrWrap = document.getElementById('qrWrap');
  const qrCard = document.getElementById('qrSmartCard');
  const qrBurst = document.getElementById('qrBurst');
  const ua = (navigator.userAgent || '').toLowerCase();
  const isAndroid = ua.includes('android');
  const isIos = ua.includes('iphone') ||
    ua.includes('ipad') ||
    ua.includes('ipod') ||
    (ua.includes('macintosh') && ua.includes('mobile'));
  const isMobile = isAndroid || isIos;
  const isDesktop = !isMobile;

  const hasPlay = !!play;
  const hasApp = !!app;

  const setLink = (el, url) => {
    if (!el) return;
    if (url) {
      el.href = url;
      el.style.display = 'inline-flex';
    } else {
      el.style.display = 'none';
    }
  };

  const showPlay = hasPlay && (!isMobile || isAndroid);
  const showApp = hasApp && (!isMobile || isIos);

  const appWithParams = appendParams(app, {
    lang: currentLang || 'en',
    public: publicUrl
  });
  const appFinal = ensureAppStoreIndex(appWithParams);
  const resolvedPlay = resolveUrl(play);
  const resolvedApp = resolveUrl(appFinal);

  setLink(playCta, showPlay ? resolvedPlay : '');
  setLink(playCtaBottom, showPlay ? resolvedPlay : '');
  setLink(appCta, showApp ? resolvedApp : '');
  setLink(appCtaBottom, showApp ? resolvedApp : '');

  if (auto && isMobile) {
    if (isAndroid && resolvedPlay) {
      startAutoRedirect(resolvedPlay, 'android', deeplinkAndroid);
    } else if (isIos && resolvedApp) {
      startAutoRedirect(resolvedApp, 'ios', deeplinkIos);
    }
  }

  if (qrWrap) {
    if (isDesktop && (hasPlay || hasApp)) {
      const smartUrl = buildSmartUrl(
        play,
        app,
        currentLang || 'en',
        publicUrl,
        deeplinkAndroid,
        deeplinkIos,
        iosLive
      );
      if (smartQr) {
        smartQr.src = `https://api.qrserver.com/v1/create-qr-code/?size=180x180&data=${encodeURIComponent(smartUrl)}`;
      }
      qrWrap.classList.add('active');
      enableQrCardInteraction(qrCard, qrBurst);
    } else {
      qrWrap.classList.remove('active');
      if (qrCard) {
        qrCard.classList.remove('scanning');
      }
    }
  }
}

function resolveUrl(url) {
  if (!url) return '';
  if (/^[a-z][a-z0-9+.-]*:/i.test(url)) {
    return url;
  }
  const protocol = window.location.protocol;
  if (protocol === 'file:') {
    let resolved = url;
    if (resolved.startsWith('/mobile/')) {
      resolved = resolved.replace('/mobile/', './');
    } else if (resolved.startsWith('/')) {
      resolved = `.${resolved}`;
    }
    const [pathPart, hashPart] = resolved.split('#');
    const [pathOnly, queryPart] = pathPart.split('?');
    if (pathOnly.endsWith('/app-store') || pathOnly.endsWith('/app-store/')) {
      const basePath = pathOnly.endsWith('/') ? pathOnly : `${pathOnly}/`;
      const withIndex = `${basePath}index.html`;
      const withQuery = queryPart ? `${withIndex}?${queryPart}` : withIndex;
      return hashPart ? `${withQuery}#${hashPart}` : withQuery;
    }
    return resolved;
  }
  const base = window.location.origin.replace(/\/$/, '');
  if (url.startsWith('/')) return `${base}${url}`;
  return `${base}/${url}`;
}

function appendParams(url, params) {
  if (!url) return url;
  const [pathAndQuery, hash] = url.split('#');
  const [base, query] = pathAndQuery.split('?');
  const searchParams = new URLSearchParams(query || '');
  Object.entries(params).forEach(([key, value]) => {
    if (value) {
      searchParams.set(key, value);
    }
  });
  const queryString = searchParams.toString();
  const next = `${base}${queryString ? `?${queryString}` : ''}`;
  return hash ? `${next}#${hash}` : next;
}

function ensureAppStoreIndex(url) {
  if (!url) return url;
  const [pathAndQuery, hash] = url.split('#');
  const [base, query] = pathAndQuery.split('?');
  if (base.endsWith('/app-store') || base.endsWith('/app-store/')) {
    const normalized = base.endsWith('/') ? base : `${base}/`;
    const withIndex = `${normalized}index.html`;
    const withQuery = query ? `${withIndex}?${query}` : withIndex;
    return hash ? `${withQuery}#${hash}` : withQuery;
  }
  return url;
}

function ensureBurstParticles(container) {
  if (!container) return;
  if (container.children.length) return;
  for (let i = 0; i < BURST_PARTICLE_COUNT; i += 1) {
    const dot = document.createElement('span');
    const angle = (360 / BURST_PARTICLE_COUNT) * i;
    dot.style.setProperty('--burst-angle', `${angle}deg`);
    container.appendChild(dot);
  }
}

function triggerBurst(container) {
  if (!container) return;
  ensureBurstParticles(container);
  container.classList.remove('active');
  void container.offsetWidth;
  container.classList.add('active');
}

function enableQrCardInteraction(card, burst) {
  if (!card || card.dataset.enhanced === '1') return;
  ensureBurstParticles(burst);
  card.dataset.enhanced = '1';
  card.classList.add('scanning');
  triggerBurst(burst);
  window.setTimeout(() => {
    card.classList.remove('scanning');
  }, 1200);

  card.addEventListener('mouseenter', () => {
    card.classList.add('scanning');
  });
  card.addEventListener('mouseleave', () => {
    card.classList.remove('scanning');
  });
  card.addEventListener('click', () => {
    card.classList.add('scanning');
    triggerBurst(burst);
    window.setTimeout(() => {
      card.classList.remove('scanning');
    }, 900);
  });
}

function startAutoRedirect(url, kind, deepLink) {
  if (!url) return;
  if (autoRedirectStarted) return;
  autoRedirectStarted = true;
  const prefersReduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  if (prefersReduced) {
    if (deepLink) {
      attemptDeepLink(deepLink, url);
    } else {
      window.location.href = url;
    }
    return;
  }
  const overlay = document.getElementById('scanOverlay');
  const scanCard = overlay ? overlay.querySelector('.scan-card') : null;
  const androidLogo = document.getElementById('scanLogoAndroid');
  const iosLogo = document.getElementById('scanLogoIos');
  const scanBurst = document.getElementById('scanBurst');
  if (androidLogo) {
    androidLogo.style.display = kind === 'android' ? 'flex' : 'none';
  }
  if (iosLogo) {
    iosLogo.style.display = kind === 'ios' ? 'flex' : 'none';
  }
  ensureBurstParticles(scanBurst);
  if (scanCard) {
    scanCard.classList.remove('scanning', 'complete');
  }
  if (scanBurst) {
    scanBurst.classList.remove('active');
  }
  if (overlay) {
    overlay.classList.add('active');
  }
  window.requestAnimationFrame(() => {
    if (scanCard) {
      scanCard.classList.add('scanning');
    }
  });
  window.setTimeout(() => {
    if (scanCard) {
      scanCard.classList.add('complete');
    }
    triggerBurst(scanBurst);
  }, 980);

  const redirect = () => {
    if (deepLink) {
      attemptDeepLink(deepLink, url);
      return;
    }
    window.location.href = url;
  };

  window.setTimeout(() => {
    redirect();
  }, AUTO_REDIRECT_DELAY_MS);
}

function attemptDeepLink(deepLink, fallbackUrl) {
  let completed = false;
  let timerId = null;
  const cleanup = () => {
    if (completed) return;
    completed = true;
    document.removeEventListener('visibilitychange', onVisibility);
    if (timerId) {
      window.clearTimeout(timerId);
      timerId = null;
    }
  };
  const onVisibility = () => {
    if (document.hidden) {
      cleanup();
    }
  };
  document.addEventListener('visibilitychange', onVisibility);
  timerId = window.setTimeout(() => {
    if (!completed) {
      cleanup();
      window.location.href = fallbackUrl;
    }
  }, 1400);
  window.location.href = deepLink;
}

function buildSmartUrl(play, app, lang, publicUrl, deepAndroid, deepIos, iosLive) {
  const base = new URL(window.location.href);
  base.search = '';
  base.hash = '';
  if (play) base.searchParams.set('play', play);
  if (app) base.searchParams.set('app', app);
  if (lang) base.searchParams.set('lang', lang);
  if (publicUrl) base.searchParams.set('public', publicUrl);
  if (deepAndroid) base.searchParams.set('deeplink_android', deepAndroid);
  if (deepIos) base.searchParams.set('deeplink_ios', deepIos);
  if (iosLive) base.searchParams.set('ios_live', '1');
  base.searchParams.set('auto', '1');
  return base.toString();
}

function initLang() {
  const params = new URLSearchParams(window.location.search);
  const requested = params.get('lang');
  const fallback = (navigator.language || 'en').toLowerCase().startsWith('fr') ? 'fr' : 'en';
  const lang = requested || fallback;
  applyLanguage(lang);
}

function observeReveals() {
  if (!revealObserver) return;
  document.querySelectorAll('.reveal').forEach((el) => {
    if (!el.classList.contains('visible')) {
      revealObserver.observe(el);
    }
  });
}

function initReveal() {
  revealObserver = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
          revealObserver.unobserve(entry.target);
        }
      });
    },
    { threshold: 0.15 }
  );
  observeReveals();
}

function initCarousel() {
  const frames = Array.from(document.querySelectorAll('.carousel-device'));
  if (!frames.length) return;

  const template = document.getElementById('carouselSlides');
  frames.forEach((frame) => {
    const screen = frame.querySelector('.carousel-screen');
    if (!screen || screen.dataset.slides) return;
    if (template) {
      screen.appendChild(template.content.cloneNode(true));
      screen.dataset.slides = 'true';
    }
    if (!screen.querySelector('.ui-statusbar')) {
      const status = document.createElement('div');
      status.className = 'ui-statusbar';
      status.innerHTML = `
        <span class="ui-time">9:41</span>
        <div class="ui-status-icons">
          <div class="ui-signal"><span></span><span></span><span></span><span></span></div>
          <div class="ui-wifi"><span></span><span></span></div>
          <div class="ui-battery"></div>
        </div>
      `;
      screen.appendChild(status);
    }
    const isIos = frame.classList.contains('ios');
    if (isIos) {
      if (!screen.querySelector('.ui-homebar')) {
        const home = document.createElement('div');
        home.className = 'ui-homebar';
        screen.appendChild(home);
      }
    } else if (!screen.querySelector('.ui-navbar')) {
      const nav = document.createElement('div');
      nav.className = 'ui-navbar';
      nav.innerHTML = `
        <span class="ui-nav-item active"></span>
        <span class="ui-nav-item"></span>
        <span class="ui-nav-item"></span>
        <span class="ui-nav-item"></span>
      `;
      screen.appendChild(nav);
    }
  });

  const dotsWrap = document.getElementById('carouselDots');
  const prev = document.querySelector('[data-action="prev"]');
  const next = document.querySelector('[data-action="next"]');
  const phoneWrap = document.getElementById('carouselPhones');
  const slideCount = frames[0].querySelectorAll('.carousel-slide').length;
  if (!slideCount) return;
  const tags = Array.from(document.querySelectorAll('.carousel-tag'));
  const dots = [];
  if (dotsWrap) {
    dotsWrap.innerHTML = '';
    for (let i = 0; i < slideCount; i += 1) {
      const dot = document.createElement('button');
      dot.className = `carousel-dot${i === 0 ? ' active' : ''}`;
      dot.dataset.slide = String(i);
      dot.setAttribute('aria-label', `Screen ${i + 1}`);
      dotsWrap.appendChild(dot);
      dots.push(dot);
    }
  }
  let index = 0;
  let timerId = null;
  let startX = null;

  const setIndex = (nextIndex) => {
    index = (nextIndex + slideCount) % slideCount;
    frames.forEach((frame) => {
      frame.querySelectorAll('.carousel-slide').forEach((slide) => {
        slide.classList.toggle('active', Number(slide.dataset.slide) === index);
      });
    });
    dots.forEach((dot) => {
      dot.classList.toggle('active', Number(dot.dataset.slide) === index);
    });
    tags.forEach((tag) => {
      tag.classList.toggle('active', Number(tag.dataset.slide) === index);
    });
  };

  const go = (delta) => setIndex(index + delta);

  prev?.addEventListener('click', () => go(-1));
  next?.addEventListener('click', () => go(1));
  dots.forEach((dot) => {
    dot.addEventListener('click', () => setIndex(Number(dot.dataset.slide)));
  });
  tags.forEach((tag) => {
    tag.addEventListener('click', () => setIndex(Number(tag.dataset.slide)));
  });

  const stopAuto = () => {
    if (timerId) {
      window.clearInterval(timerId);
      timerId = null;
    }
  };

  const startAuto = () => {
    if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;
    stopAuto();
    timerId = window.setInterval(() => go(1), 6500);
  };

  phoneWrap?.addEventListener('pointerdown', (event) => {
    startX = event.clientX;
    stopAuto();
  });
  phoneWrap?.addEventListener('pointerup', (event) => {
    if (startX == null) return;
    const delta = event.clientX - startX;
    if (Math.abs(delta) > 40) {
      go(delta > 0 ? -1 : 1);
    }
    startX = null;
    startAuto();
  });
  phoneWrap?.addEventListener('pointerleave', () => {
    startX = null;
    startAuto();
  });
  phoneWrap?.addEventListener('pointercancel', () => {
    startX = null;
    startAuto();
  });

  phoneWrap?.addEventListener('mouseenter', stopAuto);
  phoneWrap?.addEventListener('mouseleave', startAuto);

  setIndex(index);
  startAuto();
}

function initCounters() {
  const counters = Array.from(document.querySelectorAll('[data-count]'));
  if (!counters.length) return;

  const seen = new WeakSet();
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (!entry.isIntersecting) return;
        const el = entry.target;
        if (seen.has(el)) return;
        seen.add(el);
        const target = Number(el.dataset.count || 0);
        const duration = 1200;
        const start = performance.now();

        const tick = (now) => {
          const progress = Math.min((now - start) / duration, 1);
          const eased = 1 - Math.pow(1 - progress, 3);
          el.textContent = String(Math.round(target * eased));
          if (progress < 1) {
            requestAnimationFrame(tick);
          }
        };

        requestAnimationFrame(tick);
        observer.unobserve(el);
      });
    },
    { threshold: 0.4 }
  );

  counters.forEach((el) => observer.observe(el));
}

window.addEventListener('DOMContentLoaded', () => {
  initLang();
  setStoreLinks();
  initCarousel();
  initReveal();
  initCounters();
  document.querySelectorAll('.lang-btn').forEach((btn) => {
    btn.addEventListener('click', () => {
      applyLanguage(btn.dataset.lang);
      setStoreLinks();
    });
  });
});
