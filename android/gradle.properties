# New Code

# Lower the memory to 1.5GB to leave room for Windows and Flutter
org.gradle.jvmargs=-Xmx1536M -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError -XX:+UseParallelGC

# Essential for modern Flutter
android.useAndroidX=true
android.enableJetifier=true

# Speed up builds on low RAM
org.gradle.daemon=true
org.gradle.parallel=false
org.gradle.configureondemand=true
org.gradle.caching=true

# Memory management for Kotlin
kotlin.incremental=true
kotlin.daemon.jvm.options=-Xmx512M















# Old Code
# org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
# android.useAndroidX=true
